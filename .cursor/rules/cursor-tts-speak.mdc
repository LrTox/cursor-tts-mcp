---
description: cursor-tts 进展/结果/决策播报（仓库副本）
alwaysApply: true
---

# 语音播报 cursor-tts（必须遵守）

有实质进展、任务结论、报错或需用户操作时，**立即**调用 MCP `speak`，短句≤80字，**语言跟随系统 UI**（勿强制中文）。禁止只打字。

详见用户级规则：`%USERPROFILE%\.cursor\rules\cursor-tts-speak.mdc`
